services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aspnet10-api
    ports:
      - "5000:5000"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://0.0.0.0:5000
    restart: unless-stopped
